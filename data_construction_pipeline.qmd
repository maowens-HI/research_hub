---
title: "Data Construction Pipeline"
subtitle: "Building the District, Tract, and County Panels for School Finance Analysis"
author: "Myles Owens"
date: "2026-01-22"
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
    code-fold: true
---

## Executive Summary

Our goal is to replicate Jackson, Johnson, and Persico (2016) as faithfully as possible given our data limitations and some methodological differences. This document details how we constructed the data and replicated the analysis up to the first stage of JJP, which corresponds to their event-study estimates used to construct Figure 1 in the original paper. Replicating JJP required building a complex data pipeline, and we found it essential to document each step to understand exactly which units enter our regressions and why others are excluded.

This document is meant to help readers understand the data pipeline. Those who simply wish to run our code should see the README located in the repository: [github.com/maowens-HI/school_finance](https://github.com/maowens-HI/school_finance).

### Pipeline Overview 

Each stage in the data pipeline corresponds to a distinct Stata do file. At the end of each stage section, you will find a diagram illustrating the data flow and key operations for that stage. A comprehensive diagram showing all stages together appears at the bottom of the document.

**[Stage 1: District Panel Construction.](#stage1)** Historical and modern finance data use different district identifier systems, so we build a crosswalk linking modern (LEAID) and historical (GOVID) identifiers, retaining only clean 1:1 mappings. After appending the panels and linking to Census geography via the GRF, we have 9,428 districts and 424,404 observations. This stage also creates baseline spending flags indicating whether each district has valid data for 1967, 1970, 1971, and/or 1972.

**[Stage 2: Tract Panel Construction.](#stage2)** Assigns each tract to the district serving the largest share of its population. Propagates baseline spending flags using the MIN rule, so a tract is marked as good only if all serving districts have valid baseline data.

**[Stage 3: County Baseline Spending Tagging.](#stage3)** Collapses tract-level baseline spending flags to counties using the MIN function. A county is marked as good only if all of its tracts have complete baseline data. This file is merged into the final analysis dataset so that regressions can exclude counties without good baseline spending.

**[Stage 4: County Panel Construction.](#stage4)** Interpolates small gaps (three years or fewer) in district spending, adjusts for inflation using CPI-U, and collapses to county using school-age population weights. Merges JJP reform treatment data and 1970 Census median family income.

**[Stage 5: Sample Restrictions and Analysis Variables.](#stage5)** Creates spending variables (winsorized per-pupil expenditure [PPE], log PPE, 13-year rolling mean), constructs event-time indicators, and computes baseline spending quartiles within each state. Enforces a balanced panel requirement so that counties must have data for all 23 years of the event window (-5 to +17).

**[Stage 6: Event-Study Analysis.](#stage6)** Runs regressions by baseline spending quartile (Q1–Q4) with county and year fixed effects and clustered standard errors. Produces Figure 1 plots showing spending trajectories before and after reform.

### Attrition

The final analysis sample contains 1,587 counties across 35 states (strict balance on `lexp_ma_strict`) or 1,986 counties across 43 states (relaxed balance on `lexp`). See the [Sample Attrition](#sample-attrition) section at the end of this document for a full breakdown of sample restrictions at each stage.

---

# Stage 1: District Panel Construction {#stage1}

Stage 1 unifies historical and modern school finance data into a single district-year panel. The key challenge in unification is that historical data and modern data use two different district identifier systems. The historical data from the Census' Individual Local Government Finances (INDFIN) uses the Government ID standard (GOVID) while modern data from the National Center for Education Statistics (NCES) Local Education Agency Finance Survey (f-33) uses the Local Education Agency ID standard (LEAID). Fortunately, we are able to construct a crosswalk from the f-33 data as it includes links between each LEAID to each GOVID. This crosswalk enables us to append historical and modern data, and ultimately link everything to Census geography for county-level aggregation.

This seems like it would be a straightforward process, but there is a major caveat: each LEAID does not always have a one-to-one match in the GOVID system. We will elaborate further in the subsequent section.

**Table 1: Stage 1 Data Sources**

| Source | Years | Identifier | Coverage |
|--------|-------|------------|----------|
| F-33 (NCES [National Center for Education Statistics] Local Education Agency Finance Survey) | 1992–2022 | LEAID (Local Education Agency ID) | Modern finance data |
| INDFIN (Individual Local Government Finances) | 1967–1991 | GOVID (Government ID) | Historical finance data |
| GRF (Geographic Reference File) | 1969 | LEAID | Links districts to Census tracts |

## Step 1: Build the LEAID↔GOVID Crosswalk

Not every LEAID maps to exactly one GOVID. There are a variety of reasons why this can happen. Districts split, merge, and change codes over time, creating one-to-many and many-to-many relationships. However, there is a conservative solution to this problem: we keep only the clean one-to-one pairs. Concretely, this means building a dataset of only districts where one LEAID corresponds to exactly one GOVID across all years.

**Why drop non-one-to-one relationships?** Ambiguous mappings prevent us from consistently tracking a district over time. If LEAID "A" maps to both GOVID "X" and GOVID "Y" in different years, we cannot reliably link that district's historical and modern spending data.

After filtering, we retain 15,047 clean one-to-one pairs in the `xwalk_leaid_govid` crosswalk.

```
Total LEAID↔GOVID pairs in F-33 ID file
              │
              ▼
     ┌────────┴────────┐
     │  1. Drop junk   │
     │  LEAIDs & GOVIDs│
     │  ("M","N",miss) │
     └────────┬────────┘
              │
              ▼
     ┌────────┴────────┐
     │  2. Filter by   │
     │  relationship   │
     │  type           │
     └────────┬────────┘
              │
    ┌─────────┼─────────┬─────────┐
    ▼         ▼         ▼         ▼
  m:m       1:m       m:1       1:1
 (drop)   (drop)    (drop)   (keep)


```



**Table 2: LEAID-GOVID Relationship Types**

| Relationship | Example | Action |
|--------------|---------|--------|
| **1:1** | LEAID A ↔ GOVID X | Keep |
| **1:m** | LEAID A → GOVID X, Y | Drop |
| **m:1** | LEAID A, B → GOVID X | Drop |
| **m:m** | LEAID A, B ↔ GOVID X, Y | Drop |



## Step 2: Add LEAIDs to INDFIN Panel

As stated previously, the INDFIN panel contains historical spending data identified only by GOVID. To link it with modern data and Census geography, we must assign LEAIDs to each GOVID. The first step is to get a list of every GOVID in INDFIN. To do this, we collapse the INDFIN panel by GOVID to create govtag, giving us a file with one row per GOVID. This sets us up to use our crosswalk to assign LEAIDs.

Remember that our crosswalk only contains GOVIDs and LEAIDs with one-to-one relationships. Therefore, we expect a portion of IDs from `govtag` to go unmatched. The merge attaches quality flags from INDFIN to the crosswalk pairs. After dropping unmatched GOVIDs (those not in the crosswalk), we retain the full 15,047 LEAID-GOVID pairs from the crosswalk. Of these, 13,773 have INDFIN spending data and inherit their quality flags; the remaining 1,274 crosswalk pairs have no INDFIN data, so their flags are set to zero.


```
govtag                          xwalk_leaid_govid
(GOVID + quality flags)         (LEAID ↔ GOVID)
[MASTER]                        [USING]
     │                                │
     └──────────── MERGE ─────────────┘
                     │
                     ▼
           ┌─────────────────┐
           │ Merge Results:  │
           │ • Master: X     │ ← GOVIDs not in crosswalk (drop)
           │ • Using:  1,274 │ ← Crosswalk pairs not in INDFIN (keep, flags=0)
           │ • Match: 13,773 │ ← GOVIDs matched to LEAIDs (keep)
           └─────────────────┘
                     │
                     ▼
           15,047 LEAID-GOVID pairs
           (= size of crosswalk)

```

### Baseline year flags

Before we move on to the next step of our data cleaning process we must first mention another important step concerning the INDFIN panel and `govtag`. During this part in the pipeline, before we collapse INDFIN into `govtag`, we create flags indicating whether each GOVID has valid spending in what we call the baseline spending years (1967, 1970, 1971, 1972). The name stems from the fact that we are concerned with the spending of each district during the beginning period of INDFIN, before the exogenous shocks central to our research design occurred.

Baseline spending plays a critical role in our event-study analysis. The event-study design compares how reforms affected districts with different initial spending levels. Districts in the bottom quartile (Q1) had the lowest pre-reform spending; districts in the top quartile (Q4) had the highest. Without baseline spending data, we cannot classify districts into quartiles and cannot test for heterogeneous treatment effects.

If a district lacks valid spending data for the baseline years, we cannot include it in the regression. The quality flags (`good_govid_1967`, `good_govid_1970`, etc.) track this. These flags propagate through the pipeline, from districts to tracts to counties, ensuring our final sample contains only observations with complete baseline histories.

This explains why we retain the crosswalk-only pairs from the previous merge. Those pairs have valid LEAIDs, so they can still be linked to census tracts. Their flags propagate through the pipeline like any other observation. But since their flags are set to zero, they will be excluded from analyses requiring baseline spending.


## Step 3: Restrict F-33 Panel to Crosswalk

Steps 2 and 3 are parallel operations on opposite ends of our panel. Step 2 attached LEAIDs to historical INDFIN data (1967–1991); Step 3 restricts modern F-33 data (1992–2022) to the same set of clean identifiers.

Without this restriction, we would include districts that we cannot reliably track across the historical/modern data boundary. For example, if a district's LEAID maps to multiple GOVIDs, we cannot determine which historical spending record (if any) corresponds to the modern district. By restricting to the crosswalk, we ensure that every district in our final panel has a consistent identifier across all years.

As with the data set in step 2, any observation in the F33 panel whose LEAID does not appear in the crosswalk gets dropped. This leaves us with 15,047 district-year observations, all of which share identifiers with our restricted historical data. The two panels are now ready to be appended into a single district-year panel spanning 1967–2022.

```
xwalk_leaid_govid               F-33 Panel
(LEAID ↔ GOVID)                 (district-year obs)
[MASTER]                        [USING]
     │                                │
     └──────────── MERGE ─────────────┘
                     │
                     ▼
           ┌──────────────────┐
           │ Merge Results:   │
           │ • Master:  0     │ ← Crosswalk pairs not in F-33
           │ • Using: 10,236  │ ← F-33 obs without clean crosswalk (drop)
           │ • Match: 15,047  │ ← Kept observations
           └──────────────────┘
```



## Step 4: Append Historical and Modern Panels

With both panels now restricted to consistent identifiers, we can combine them. At this point, both panels contain the same identifier variables (LEAID and GOVID), the same spending variable (per-pupil expenditure), and the same quality flags. The panels differ only in their time coverage: INDFIN covers the historical period (1967-1991) and F-33 covers the modern period (1992-2022).

```
F-33 Panel (restricted)      INDFIN Panel (with LEAIDs)
   1992–2022                     1967–1991
        │                            │
        └──────────┬─────────────────┘
                   │
                   ▼
              APPEND
                   │
                   ▼
           dist_panel
        ━━━━━━━━━━━━━━━━━━━━━
        605,245 observations
        15,047 unique LEAIDs
        15,047 unique GOVIDs
        Years: 1967–2022
```


The append confirms our crosswalk worked as intended. The 15,047 unique LEAIDs exactly equals the 15,047 unique GOVIDs: each district has one identifier in each system, no exceptions. Additionally, because INDFIN ends in 1991 and F-33 begins in 1992, there is no year overlap; the append is clean.

**Data limitations to note:**

The panel is unbalanced: not every district appears in every year due to districts entering or exiting the data over time. Additionally, INDFIN contains no data for 1968 or 1969, creating a gap between 1967 and 1970.

## Step 5: Link to Census Geography (GRF Merge)

The final step of Stage 1 connects our district finance data to Census geography. The Geographic Reference File (GRF) from 1969 provides the link between school district boundaries (identified by LEAID) and 1970 Census tracts. This geographic linkage is essential for three reasons.

First, our analysis occurs at the county level. While the original JJP study was at the district level, our replication analyzes spending at the county level. Counties provide a stable geographic unit over time since county boundaries change rarely, and they align well with the administrative data we have access to.

Second, tracts nest cleanly within counties; every tract belongs to exactly one county. By linking districts to tracts, and then tracts to counties in Stage 2, we can compute county-level spending measures through straightforward aggregation.

Third, the GRF provides consistent geographic definitions over time. The 1969 GRF captures district boundaries as of the 1969–70 school year, which means districts that formed later, dissolved earlier, or underwent major reorganization may not appear. We accept this limitation because it mirrors the approach used in JJP and gives us a stable geographic foundation for the entire panel.

With these motivations in mind, we merge the district panel with the GRF on LEAID:

```
GRF-ID                          dist_panel
(LEAID → tract70)               (LEAID + GOVID + spending)
[MASTER]                        [USING]
     │                                │
     └──────────── MERGE ─────────────┘
                     │
                     ▼
           ┌──────────────────┐
           │ Merge Results:   │
           │ • Master:  2,932 │ ← GRF LEAIDs not in panel (drop)
           │ • Using: 5,611   │ ← Panel obs without GRF match (drop)
           │ • Match: 9,436   │ ← Final canonical panel
           └──────────────────┘
                     │
                Minor Cleaning
                     |
                     ▼
             dist_panel
         ━━━━━━━━━━━━━━━━━━━━
         424,404 observations
         9,428 unique LEAIDs
         Final canonical panel
```

**Understanding the merge results**

The merge drops a substantial number of observations on both sides, so it's worth understanding what we're losing and why.
 
The 2,932 master-only records represent districts that appear in the GRF but have no finance data in our panel. These districts may have had non-1:1 LEAID-GOVID mappings (and were therefore dropped in Step 1) or may have been small or short-lived districts not consistently tracked in the finance data.
 
The 5,611 using-only districts are districts from our finance panel that we cannot link to Census geography. This may occur because these districts operated in areas not covered by the 1969 GRF, either because they formed after 1969 or because their boundaries did not contain any tracts in that year's geographic file.
 
These dropped observations could potentially be recovered at a later through various means. This may be important if we later determine that the coverage gaps are bias our analysis.


### Stage 1 Output: dist_panel.dta

**Table 3: Stage 1 Output Variables**

| Variable | Description |
|----------|-------------|
| `LEAID` | NCES Local Education Agency ID (7-character string) |
| `GOVID` | Census Government ID (9-character string) |
| `tract70` | 1970 Census tract (11-character string) |
| `year` | Fiscal year |
| `pp_exp` | Per-pupil expenditure (nominal dollars) |
| `totalexp` | Total expenditure |
| `enrollment` | Student enrollment |
| `good_govid` | District has complete baseline data (1967, 1970–1972) |
| `good_govid_1967` | Has valid 1967 spending data |
| `good_govid_1970` | Has valid 1970 spending data |
| `good_govid_1971` | Has valid 1971 spending data |
| `good_govid_1972` | Has valid 1972 spending data |

## Stage 1 Pipeline Diagram {#stage1-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    F33P["<b>f33-panel</b><br/>1992-2019<br/>Modern finance data"]
    INDFIN["<b>Indfin-panel</b><br/>1967-1991<br/>GOVID only"]
    F33ID["<b>f33-ID</b><br/>Crosswalk source<br/>LEAID + GOVID"]
    GRFID["<b>GRF-id</b><br/>12,368 LEAIDs<br/>Census geography"]

    STEP1_LABEL["<b>STEP 1: CROSSWALK FILTERING</b>"]
    FILTER["Filter to 1:1<br/>LEAID-GOVID"]
    MM["m:m (drop)"]
    ONEM["1:m (drop)"]
    MONE["m:1 (drop)"]
    ONEONE["<b>1:1: 15,047</b><br/>KEEP"]

    F33_1TO1["<b>xwalk_leaid_govid</b><br/>15,047 LEAID-GOVID pairs"]

    GOVTAG["<b>govtag</b><br/>GOVIDs from INDFIN<br/>+ quality flags"]

    STEP2_LABEL["<b>STEP 2: MERGE govtag x xwalk_leaid_govid</b>"]
    M1_RESULTS["_merge=1 (master): X<br/>(GOVIDs not in crosswalk - drop)<br/>_merge=2 (using): 1,274<br/>(Crosswalk pairs not in INDFIN - keep, flags=0)<br/>_merge=3 (match): 13,773"]

    INDFIN_W_LEAID["<b>INDFIN + LEAID</b><br/>15,047 LEAID-GOVID pairs"]

    STEP3_LABEL["<b>STEP 3: MERGE xwalk_leaid_govid x f33-panel</b>"]
    M2_RESULTS["_merge=1 (master): 0<br/>_merge=2 (using): dropped<br/>(F-33 obs without clean crosswalk)<br/>_merge=3 (match): 15,047"]

    F33_RESTRICTED["<b>f33-panel (restricted)</b><br/>15,047 unique LEAIDs"]

    STEP4_LABEL["<b>STEP 4: APPEND</b>"]

    DIST_PANEL["<b>dist_panel</b><br/>605,245 obs<br/>15,047 unique LEAIDs<br/>15,047 unique GOVIDs<br/>1967-2022"]

    STEP5_LABEL["<b>STEP 5: MERGE GRF x dist_panel</b>"]
    M3_RESULTS["_merge=1 (master): 2,932 drop<br/>(GRF LEAIDs not in panel)<br/>_merge=2 (using): 5,611 drop<br/>(Panel obs without GRF match)<br/>_merge=3 (match): 9,436"]

    FINAL["<b>dist_panel</b><br/>424,404 obs<br/>9,428 unique LEAIDs<br/>(76% of GRF)"]

    F33ID --> STEP1_LABEL
    STEP1_LABEL --> FILTER
    FILTER --> MM
    FILTER --> ONEM
    FILTER --> MONE
    FILTER --> ONEONE
    ONEONE --> F33_1TO1

    INDFIN --> GOVTAG
    GOVTAG --> STEP2_LABEL
    F33_1TO1 --> STEP2_LABEL
    STEP2_LABEL --> M1_RESULTS
    M1_RESULTS --> INDFIN_W_LEAID

    F33_1TO1 --> STEP3_LABEL
    F33P --> STEP3_LABEL
    STEP3_LABEL --> M2_RESULTS
    M2_RESULTS --> F33_RESTRICTED

    F33_RESTRICTED --> STEP4_LABEL
    INDFIN_W_LEAID --> STEP4_LABEL
    STEP4_LABEL --> DIST_PANEL

    GRFID --> STEP5_LABEL
    DIST_PANEL --> STEP5_LABEL
    STEP5_LABEL --> M3_RESULTS
    M3_RESULTS --> FINAL

    %% Styling - high contrast colors
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef mergeResults fill:#f3e5f5,stroke:#7b1fa2,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000
    classDef dropBox fill:#ffebee,stroke:#c62828,stroke-width:1px,color:#000000
    classDef keepBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000

    class F33P,INDFIN,F33ID,GRFID sourceBox
    class STEP1_LABEL,STEP2_LABEL,STEP3_LABEL,STEP4_LABEL,STEP5_LABEL stepLabel
    class FILTER filterBox
    class MM,ONEM,MONE dropBox
    class ONEONE keepBox
    class M1_RESULTS,M2_RESULTS,M3_RESULTS mergeResults
    class F33_1TO1,GOVTAG,INDFIN_W_LEAID,F33_RESTRICTED,DIST_PANEL outputBox
    class FINAL finalBox
```

---

# Stage 2: Tract Panel Construction {#stage2}

Stage 1 produced a district-year panel with consistent identifiers and quality flags. Stage 2 transforms this into a tract-year panel, an intermediate step essential for constructing county-level spending measures.

### The Core Challenge

The complication is that multiple districts can serve the same tract. In many areas, separate districts handle primary and secondary schools. Some tracts fall within overlapping district boundaries. For clean county aggregation, we need exactly one spending value per tract.

Our solution is to assign each tract to the district serving the largest share of its population. The 1969 Geographic Reference File enables us to calculate how much of a tracts population is served by each associated district. We make the district serving the largest portion of a tracts population the "winning" district. The following sections walk through this process in detail.

## Data

**Table 4: Stage 2 Data Sources**

| Dataset | Purpose |
|---------|---------|
| **GRF 1969** | Links tracts to districts with population weights |
| **dist_panel.dta** | District spending and quality flags (from Stage 1) |


## Key Operations

### Step 1: GRF Import and Cleaning

The first step prepares the Geographic Reference File for use. We import the raw GRF, drop special geographies (water bodies, Native lands, ships, tract revisions), and construct standardized identifiers.

**Table 5: Geographic Identifier Construction**

| ID | Components | Length | Example |
|----|------------|--------|---------|
| `tract70` | state(2) + county(3) + btc(4) + tsc(2) | 11 chars | 01001000100 |
| `LEAID` | state(2) + sdc(5) | 7 chars | 0100005 |

Here `btc` = basic tract code, `tsc` = tract suffix code, and `sdc` = school district code.


### Understanding Allocated Population

A key variable we create from the GRF is "allocated population." The raw GRF is at the block/ED level. Each record shows the population of a block/ED (`popc`) and the percentage of that block/ED served by a given district (`perc`). Allocated population is the product:

$$
\text{alloc\_pop} = \text{popc} \times \frac{\text{perc}}{100}
$$

We collapse the block/ED-level data to the tract × district level by summing `alloc_pop`. This gives us the total population in each tract served by each district.

**Table 6: Allocated Population Example**

| Tract | Block/ED | popc | District | perc | alloc_pop |
|-------|----------|------|----------|------|-----------|
| 001   | A        | 600  | District 1 | 60% | 360 |
| 001   | A        | 600  | District 2 | 40% | 240 |
| 001   | B        | 400  | District 1 | 100% | 400 |

After collapsing by tract × district: District 1 serves 760 people (360 + 400), District 2 serves 240 people. District 1 "wins" because it serves more people, and the tract inherits District 1's spending data.

Occasionally two or more districts have identical `alloc_pop` values for the same tract. We need a tiebreaker. The code sorts by LEAID, so the district with the lower LEAID value wins.

**Note on `sdtc`:** The school district type code (`sdtc`) classifies districts by the grade levels they serve: 1 = unified (K-12), 2 = elementary only, 3 = secondary only, 4 = vocational. This classification matters because a single tract may be served by separate elementary and secondary districts. The winner selection actually happens within each district type, so a tract can have up to three assigned districts (one unified, one elementary, one secondary).


### Step 2: Tract-District Assignment

Assignment involves three operations.

First, we collapse `alloc_pop` to the tract × district × type level. This aggregates the block-level data up to tracts while preserving district type distinctions.

Second, we drop vocational districts (`sdtc` = 4). These are not relevant for K-12 analysis and would complicate the tract assignment.

Third, we pick the winner. For each tract × type combination, we keep only the district with the highest `alloc_pop`. This means a tract can have up to three assigned districts: one unified (sdtc=1), one elementary (sdtc=2), and one secondary (sdtc=3). In practice, most tracts are served by either a single unified district or by separate elementary and secondary districts.

### Propagating Quality Flags

We also need to propagate district-level quality flags to tracts. We use a conservative rule: a tract is "good" for a given baseline year only if ALL districts serving it have valid data for that year. The formula is:

$$
\text{good\_tract\_flag} = \min(\text{good\_govid\_flag})
$$

across all districts in the tract. We create several versions of these flags: year-specific flags (`good_tract_1967`, `good_tract_1970`, `good_tract_1971`, `good_tract_1972`) and composite flags that combine multiple years (`good_tract` for all four years, `good_tract_6771` for 1967/1970/1971, and `good_tract_7072` for 1970/1971/1972). The composite flags are computed at the district level and then propagated to tracts using the same MIN rule.

Why take the minimum? Consider a tract served by three districts: A, B, and C. Even if we assign the tract's spending to District A (the population winner), Districts B and C also serve students in this tract. If District B lacks 1970 spending data, the tract's true 1970 spending picture is incomplete; we only observe what District A spent, not what Districts B and C spent. By taking the minimum, we flag such tracts as problematic.

This conservative approach means we may exclude some tracts that would otherwise have usable data. But it ensures that tracts flagged as "good" have unambiguous, complete spending histories.

### Step 3: Final Merge

The final step joins district-year spending to tracts. We use `joinby LEAID` to link the tract-district crosswalk with the district panel from Stage 1. This expands one district-year row into multiple tract-year rows, since the same district can be assigned to multiple tracts. We then merge in the quality flags computed above.

The result is a tract-year panel where each tract has spending data inherited from its assigned district, along with quality flags indicating baseline data completeness.

## District Attrition

Tract assignment can cause units to disappear for several reasons. Districts can be dropped if all tracts associated with that district are assigned to other districts with larger populations. Larger geographies like counties can also disappear if the districts nested within a county are all assigned to districts that do not exist in the clean panel. Recall that only 76% of the districts in the GRF are present in the district panel. After tract assignment, we are left with 5,760 unique LEAIDs and 2,308 unique counties across 46 states.



## Stage 2 Output: tract_panel.dta

**Table 7: Stage 2 Output Variables**

| Variable | Description |
|----------|-------------|
| `tract70` | 1970 Census tract (11 chars) |
| `LEAID` | Assigned school district |
| `GOVID` | Census Government ID (9-character string) |
| `year4` | Fiscal year |
| `pp_exp` | Per-pupil expenditure |
| `sdtc` | School district type code (1=unified, 2=elementary, 3=secondary) |
| `good_tract_*` | Baseline spending flags propagated from districts using MIN rule |
| `state_fips` | State FIPS code (2 chars) |
| `county_code` | State + County FIPS (5 chars) |

---

## Stage 2 Pipeline Diagram {#stage2-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    GRF["<b>GRF 1969</b><br/>03515-0001-Data.txt<br/>Fixed-width ASCII<br/>34 variables"]
    DIST_PANEL["<b>dist_panel</b><br/>421,404 obs<br/>District-year panel<br/>1967-2019"]

    STEP1A_LABEL["<b>STEP 1A: IMPORT GRF</b>"]

    STEP1B_LABEL["<b>STEP 1B: DROP SPECIAL GEOGRAPHIES</b>"]
    DROP_SPECIAL["Drop if:<br/>• tsc == 99 (ships/special)<br/>• tsc 70-98 (tract revisions)<br/>• btc 9400-9499 (Native lands)<br/>• btc 9800-9899 (admin codes)<br/>• btc 9900-9998 (water)"]

    STEP1C_LABEL["<b>STEP 1C: BUILD IDs</b>"]
    BUILD_IDS["Construct:<br/>• tract70 (11 chars)<br/>• gisjoin2 (13 chars)<br/>• LEAID (7 chars)<br/>• govid (9 chars)"]

    STEP1D_LABEL["<b>STEP 1D: CALC ALLOC_POP</b>"]
    CALC_POP["alloc_pop = popc × (perc/100)<br/>Population assigned to district"]

    GRF_BLOCK["<b>grf_block.dta</b><br/>Block/ED level<br/>tract70, LEAID, alloc_pop"]

    STEP2A_LABEL["<b>STEP 2A: COLLAPSE TO TRACT×DISTRICT</b>"]
    COLLAPSE["collapse (sum) alloc_pop<br/>by tract70, sdtc, LEAID"]

    GRF_TRACT["<b>xwalk_tract_dist</b><br/>Tract × District × Type"]

    STEP2B_LABEL["<b>STEP 2B: DROP VOCATIONAL</b>"]
    DROP_VOC["drop if sdtc == 4<br/>(vocational districts)"]

    STEP2C_LABEL["<b>STEP 2C: PICK ONE LEAID PER TRACT-TYPE</b>"]
    GSORT["gsort tract70 sdtc -alloc_pop LEAID<br/>by tract70 sdtc: keep if _n==1<br/><i>Highest pop wins</i>"]

    XWALK["<b>xwalk (tempfile)</b><br/>One LEAID per tract-type<br/>isid tract70 sdtc"]

    QUAL_LABEL["<b>TAG TRACTS BY DATA QUALITY</b>"]
    QUAL_JOIN["joinby LEAID<br/>xwalk_tract_dist × dist_panel"]
    QUAL_AGG["bys tract70: egen good_tract = min(good_govid)<br/><i>Tract is good only if ALL its districts are good</i>"]
    TRACT_FLAG["<b>tract_flag (tempfile)</b><br/>tract70, good_tract flags"]

    STEP3_LABEL["<b>STEP 3: JOINBY SPENDING DATA</b>"]
    JOINBY["joinby LEAID<br/>xwalk × dist_panel"]
    M1_RESULTS["keep if _merge == 3<br/><i>Only matched records</i>"]

    STEP4_LABEL["<b>STEP 4: MERGE QUALITY FLAGS</b>"]
    MERGE_FLAGS["merge m:1 tract70<br/>using tract_flag"]
    M2_RESULTS["keep if _merge == 3"]

    FINAL["<b>tract_panel.dta</b><br/>Tract-year panel<br/>tract70, LEAID, year4<br/>pp_exp, good_tract"]

    %% Flow connections
    GRF --> STEP1A_LABEL
    STEP1A_LABEL --> STEP1B_LABEL
    STEP1B_LABEL --> DROP_SPECIAL
    DROP_SPECIAL --> STEP1C_LABEL
    STEP1C_LABEL --> BUILD_IDS
    BUILD_IDS --> STEP1D_LABEL
    STEP1D_LABEL --> CALC_POP
    CALC_POP --> GRF_BLOCK

    GRF_BLOCK --> STEP2A_LABEL
    STEP2A_LABEL --> COLLAPSE
    COLLAPSE --> GRF_TRACT

    GRF_TRACT --> STEP2B_LABEL
    STEP2B_LABEL --> DROP_VOC
    DROP_VOC --> STEP2C_LABEL
    STEP2C_LABEL --> GSORT
    GSORT --> XWALK

    %% Quality flag branch
    GRF_TRACT --> QUAL_LABEL
    DIST_PANEL --> QUAL_LABEL
    QUAL_LABEL --> QUAL_JOIN
    QUAL_JOIN --> QUAL_AGG
    QUAL_AGG --> TRACT_FLAG

    %% Final merge
    XWALK --> STEP3_LABEL
    DIST_PANEL --> STEP3_LABEL
    STEP3_LABEL --> JOINBY
    JOINBY --> M1_RESULTS

    M1_RESULTS --> STEP4_LABEL
    TRACT_FLAG --> STEP4_LABEL
    STEP4_LABEL --> MERGE_FLAGS
    MERGE_FLAGS --> M2_RESULTS
    M2_RESULTS --> FINAL

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef mergeResults fill:#f3e5f5,stroke:#7b1fa2,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000
    classDef dropBox fill:#ffebee,stroke:#c62828,stroke-width:1px,color:#000000
    classDef keepBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000

    class GRF,DIST_PANEL sourceBox
    class STEP1A_LABEL,STEP1B_LABEL,STEP1C_LABEL,STEP1D_LABEL,STEP2A_LABEL,STEP2B_LABEL,STEP2C_LABEL,STEP3_LABEL,STEP4_LABEL,QUAL_LABEL stepLabel
    class BUILD_IDS,CALC_POP,COLLAPSE,GSORT,QUAL_JOIN,QUAL_AGG,JOINBY,MERGE_FLAGS filterBox
    class DROP_SPECIAL,DROP_VOC dropBox
    class M1_RESULTS,M2_RESULTS mergeResults
    class GRF_BLOCK,GRF_TRACT,XWALK,TRACT_FLAG outputBox
    class FINAL finalBox
```


---

# Stage 3: County Quality Tagging {#stage3}

Stage 2 produced a tract-year panel with quality flags indicating baseline data completeness. Stage 3 aggregates these flags to the county level. The event-study design requires baseline data to construct spending quartiles, so we need to identify which counties have complete coverage.

## Key Operation: Conservative Collapse

We collapse tract-level quality flags to counties using the minimum. The logic mirrors the tract-level approach: a county is "good" only if ALL its tracts are "good."

$$
\text{good\_county\_YYYY} = \min(\text{good\_tract\_YYYY})
$$

across all tracts in the county. This produces two types of county quality flags.

Year-specific flags (`good_county_YYYY`) indicate whether the county has valid spending data for a specific baseline year. For example, `good_county_1970` = 1 means every tract in this county has valid 1970 spending data. If even one tract in the county lacks data for that year, the flag equals zero.

Composite flags combine multiple years. The strictest is `good_county`, which equals 1 only if the county has valid data for all four baseline years (1967, 1970, 1971, and 1972). We also create `good_county_6771` (valid for 1967, 1970, and 1971) and `good_county_7072` (valid for 1970, 1971, and 1972). These alternatives provide flexibility: a county missing 1967 data but with complete 1970–1972 coverage can still be included in analyses that use a 1971 baseline.

Quality flags propagate through three levels:

1. **District level:** `good_govid_YYYY` = 1 if the district has valid spending data for year YYYY

2. **Tract level:** `good_tract_YYYY` = min(`good_govid_YYYY`) across all districts serving the tract

3. **County level:** `good_county_YYYY` = min(`good_tract_YYYY`) across all tracts in the county

At each level, the MIN operation ensures that a single missing observation flags the entire unit as problematic.

## Why Stage 3 is Separate

Stage 3 produces a standalone file (`county_qual_flags.dta`) that is merged into the analysis dataset in Stage 5. This separation is intentional: Stage 4 interpolates missing spending data to fill small gaps, but we do not want interpolated values to affect our baseline spending flags or tract/district assignments. By computing the flags before interpolation, we ensure they reflect actual data availability rather than imputed values.

The flags are used in the final regressions to exclude counties without valid baseline spending:

```
county_panel.dta                 county_qual_flags.dta
(from Stage 4)                   (from Stage 3)
      │                                │
      └──────────── MERGE ─────────────┘
                      │
                      ▼
            analysis_panel.dta
                      │
                      ▼
              ┌───────────────┐
              │  Regressions  │
              │  exclude obs  │
              │  where good   │
              │  flag = 0     │
              └───────────────┘
```

## Stage 3 Output: county_qual_flags.dta

**Table 8: Stage 3 Output Variables**

| Variable | Description |
|----------|-------------|
| `county` | State + County FIPS (5 chars) |
| `good_county` | Composite flag: 1 if county has valid data for ALL baseline years (1967, 1970, 1971, 1972) |
| `good_county_6771` | Composite flag: 1 if county has valid data for 1967, 1970, and 1971 |
| `good_county_7072` | Composite flag: 1 if county has valid data for 1970, 1971, and 1972 |
| `good_county_1967` | Year-specific flag: 1 if all tracts have valid 1967 spending data |
| `good_county_1970` | Year-specific flag: 1 if all tracts have valid 1970 spending data |
| `good_county_1971` | Year-specific flag: 1 if all tracts have valid 1971 spending data |
| `good_county_1972` | Year-specific flag: 1 if all tracts have valid 1972 spending data |

## Stage 3 Pipeline Diagram {#stage3-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    INPUT["<b>tract_panel.dta</b><br/>Tract-year panel<br/>with good_tract flags"]

    COLLAPSE_LABEL["<b>COLLAPSE</b>"]
    COLLAPSE_OP["collapse (min) good_county = good_tract<br/>by county<br/><i>County is good only if ALL tracts are good</i>"]

    LIST["<b>List of 'good' counties</b><br/>One row per county<br/>with quality flags"]

    OUTPUT["<b>county_qual_flags.dta</b><br/>County quality tags<br/>good_county, good_county_YYYY"]

    INPUT --> COLLAPSE_LABEL
    COLLAPSE_LABEL --> COLLAPSE_OP
    COLLAPSE_OP --> LIST
    LIST --> OUTPUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000

    class INPUT sourceBox
    class COLLAPSE_LABEL stepLabel
    class COLLAPSE_OP filterBox
    class LIST outputBox
    class OUTPUT finalBox
```

---

# Stage 4: County Panel Construction {#stage4}

Stage 3 produced county-level quality tags indicating which counties have complete baseline data. Stage 4 transforms the tract panel into a county-year analysis panel. This is the most complex stage, involving four substages: interpolation and tract assignment (4.1), inflation adjustment and aggregation (4.2), reform data merge (4.3), and income data merge (4.4). Each substage builds on the previous one, culminating in `county_panel`.

**Why interpolation happens in Stage 4, not Stage 1:** We delay interpolation until after quality tagging (Stage 3) to preserve the distinction between observed and imputed data. The quality flags must reflect actual data availability, not interpolated values. Only after tagging do we fill small gaps to maximize coverage for analysis.

## Stage 4.1: District Interpolation and Tract Assignment {#stage4-1}

Before we can aggregate tracts to counties, we need to address gaps in the district spending data. The district panel contains missing values where spending data was not reported for certain years. This substage fills small gaps using linear interpolation, reassigns districts to tracts, and classifies counties by their tract composition.

### Source Datasets

**Table 9: Stage 4.1 Data Sources**

| Dataset | Purpose |
|---------|---------|
| `dist_panel.dta` | District spending (Stage 1) |
| `xwalk_tract_dist.dta` | Tract-district crosswalk (Stage 2) |
| `grf_id_tractlevel.dta` | Tract metadata with `no_tract` indicator |

### Part 1: District Interpolation

The district panel contains gaps where spending data is missing for certain years. We fill gaps of three years or fewer using linear interpolation. Larger gaps risk too much measurement error, so we leave them unfilled.

The process works as follows. First, we compute the gap size between consecutive observations for each district. Second, we flag observations where the next available year is more than three years away. Third, we apply linear interpolation only within the acceptable gap range. This ensures we never extrapolate across long periods of missing data.

**Process:**

1. Compute gap size: `gap_next` = `year[_n+1]` - `year`

2. Flag large gaps: `too_far` = 1 if `gap_next` > 3

3. Interpolate: `ipolate pp_exp year if too_far == 0`

### Part 2: Tract Assignment

After interpolation, we repeat the tract assignment process from Stage 2. We load the tract-district crosswalk, pick the highest-population LEAID per tract, and join the interpolated spending data to tracts. This gives us a tract-year panel with more complete coverage than before interpolation.

### Part 3: County Classification

Rural or sparsely populated areas lacked Census tract codes in 1970. The Census Bureau did not define tract boundaries for these "untracted areas," so the GRF records them differently than tracted areas. We must handle untracted areas carefully to avoid mismeasuring county populations and spending.

We classify counties into four types based on their tract composition.

Type 1 counties are entirely tracted. These are counties where tract boundaries are well-defined. We process them using standard aggregation: sum tract populations and compute weighted average spending.

Type 2 counties contain only a single untracted area. These are counties where the Census defined no tracts. The GRF provides a single "untracted" record for the whole county, and we use county-level population data directly.

Type 3 counties contain multiple untracted areas (two or more). Some counties have multiple distinct "untracted" areas recorded separately in the GRF. This creates ambiguity, since we cannot determine how to distribute county population across these records. We average spending across the untracted units.

Type 4 counties are mixed, containing both tracted and untracted areas. The sum of tract populations does not equal the total county population. We compute the residual population and assign it to the untracted area: `untracted_pop = county_total - sum(tract_populations)`.

With interpolated spending data and county classifications in place, we can proceed to Stage 4.2 to adjust for inflation and aggregate tracts to counties.

**Table 10: County Type Classification**

| Type | Description | Handling |
|------|-------------|----------|
| 1 | All tracted | Standard processing: aggregate tract-level data |
| 2 | Single untracted only | Treat the entire county as one unit |
| 3 | Multiple untracted (≥2) | Ambiguous boundaries; average spending across units |
| 4 | Mixed (1 untracted + tracts) | Compute residual population for untracted area |

### Stage 4.1 Output: tract_panel_interp.dta

**Table 11: Stage 4.1 Output Variables**

| Variable | Description |
|----------|-------------|
| `tract70` | 1970 Census tract (11 chars) |
| `county` | State + County FIPS (5 chars) |
| `LEAID` | Assigned school district |
| `year4` | Fiscal year |
| `pp_exp` | Per-pupil expenditure (interpolated) |
| `enrollment` | Student enrollment (interpolated) |
| `county_type` | 1=All tracted, 2=Single untracted, 3=≥2 untracted, 4=Mixed |
| `no_tract` | Indicator for untracted area |

### Stage 4.1 Pipeline Diagram {#stage4-1-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    DIST_CANON["<b>dist_panel.dta</b><br/>District-year panel<br/>1967-2019"]
    GRF_TRACT["<b>xwalk_tract_dist.dta</b><br/>Tract-district crosswalk<br/>with alloc_pop"]
    GRF_ID["<b>grf_id_tractlevel</b><br/>Tract metadata<br/>no_tract indicator"]

    IPOLATE_LABEL["<b>IPOLATE</b>"]
    IPOLATE_OP["Impute spending for<br/>gaps ≤ 3 years"]

    INTERP_D["<b>dist_panel_interp.dta</b><br/>Interpolated district panel"]

    ASSIGN_LABEL["<b>ASSIGN BASED ON ALLOC_POP</b>"]
    XWALK["<b>xwalk</b><br/>One LEAID per tract"]

    JOINBY_LABEL["<b>JOINBY</b>"]

    INTERP_T_TEMP["<b>interp_t_temp</b><br/>Tract-year panel"]

    MERGE_LABEL["<b>MERGE</b>"]

    CLASSIFY["<b>CLASSIFY COUNTIES</b><br/>by tracted/untracted status"]

    TYPE1["1: All tracted"]
    TYPE2["2: Single untracted"]
    TYPE3["3: ≥2 untracted"]
    TYPE4["Mixed: 1 untr +<br/>some tracted"]

    INTERP_T["<b>tract_panel_interp.dta</b><br/>Tract panel with<br/>county classification"]

    %% Flow
    DIST_CANON --> IPOLATE_LABEL
    IPOLATE_LABEL --> IPOLATE_OP
    IPOLATE_OP --> INTERP_D

    GRF_TRACT --> ASSIGN_LABEL
    ASSIGN_LABEL --> XWALK

    INTERP_D --> JOINBY_LABEL
    XWALK --> JOINBY_LABEL
    JOINBY_LABEL --> INTERP_T_TEMP

    INTERP_T_TEMP --> MERGE_LABEL
    GRF_ID --> MERGE_LABEL

    MERGE_LABEL --> CLASSIFY
    CLASSIFY --> TYPE1
    CLASSIFY --> TYPE2
    CLASSIFY --> TYPE3
    CLASSIFY --> TYPE4

    TYPE1 --> INTERP_T
    TYPE2 --> INTERP_T
    TYPE3 --> INTERP_T
    TYPE4 --> INTERP_T

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000
    classDef typeBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000

    class DIST_CANON,GRF_TRACT,GRF_ID sourceBox
    class IPOLATE_LABEL,ASSIGN_LABEL,JOINBY_LABEL,MERGE_LABEL,CLASSIFY stepLabel
    class IPOLATE_OP,XWALK filterBox
    class INTERP_D,INTERP_T_TEMP outputBox
    class TYPE1,TYPE2,TYPE3,TYPE4 typeBox
    class INTERP_T finalBox
```

---

## Stage 4.2: Inflation Adjustment and School-Age Population {#stage4-2}

Stage 4.1 produced a tract panel with interpolated spending in nominal dollars. Stage 4.2 converts spending to constant 2000 dollars, imports school-age population weights, and collapses tracts to counties. The result is a county-year panel with real per-pupil expenditure.

### Source Datasets

**Table 12: Stage 4.2 Data Sources**

| Dataset | Purpose |
|---------|---------|
| `tract_panel_interp.dta` | Tract panel from Stage 4.1 |
| `enroll_age_tract.csv` | NHGIS (National Historical Geographic Information System) tract-level school-age population |
| `enroll_age_county.csv` | NHGIS county-level population (for untracted areas) |
| FRED (Federal Reserve Economic Data) CPI-U (Consumer Price Index for All Urban Consumers) | Inflation adjustment |

### Step 1: Inflation Adjustment

We convert nominal spending to constant 2000 dollars using the CPI-U (Consumer Price Index for All Urban Consumers) from FRED. The formula is:

**The formula:**

$$
\text{pp\_exp\_real} = \text{pp\_exp} \times \frac{\text{CPI}_{2000}}{\text{CPI}_{FY}}
$$

where:
- $\text{pp\_exp}$ = nominal per-pupil expenditure in the given fiscal year
- $\text{CPI}_{2000}$ = CPI-U value for calendar year 2000 (our base year)
- $\text{CPI}_{FY}$ = average CPI-U value for the state's fiscal year

The adjustment accounts for state-specific fiscal year calendars. School districts report spending by fiscal year, not calendar year, and fiscal year definitions vary by state. Most states use July 1 through June 30, but some use October 1 through September 30 or other calendars. We compute the CPI for each state's fiscal year as the average CPI across the months in that fiscal year.

### Step 2: School-Age Population Import

School-age population (ages 5–17) serves as the weighting variable for county aggregation. We import tract-level population from NHGIS, summing the variables that capture all children ages 5–17 regardless of enrollment status.

### Step 3: Handling Tracted vs. Untracted Areas

Untracted areas lack tract-level Census data, so we must handle them separately. We split the data by the `no_tract` indicator: tracted areas merge with tract-level population data, while untracted areas merge with county-level population data. We then append the two pieces back together.

### Step 4: Reshape and Assign Tract-Level PPE (Per-Pupil Expenditure)

Some tracts are served by multiple district types (for example, separate elementary and secondary districts). We reshape the data wide by district type to handle these cases. For tracts with multiple district types, we compute a weighted average based on the proportion of school-age children in each grade range. If data is missing for one district type, we fall back to the available single-district value.

$$
\text{ppe\_tract} = \text{pp\_exp\_real2} \times \text{share\_primary} + \text{pp\_exp\_real3} \times \text{share\_secondary}
$$


### Step 5: Handle Mixed Counties

For Type 4 counties (those with both tracted and untracted areas), the sum of tract populations does not equal the county total. We compute the residual population and assign it to the untracted row. This avoids double-counting when we aggregate to counties.

$$
\text{residual\_pop} = \text{county\_total} - \sum(\text{tract\_populations})
$$

### Step 6: Collapse to Counties

Finally, we aggregate tract-level spending to counties using a population-weighted average. The weighting ensures that tracts with more school-age children contribute more to the county average.

**The formula:**

$$
\text{county\_exp} = \frac{\sum_{t \in \text{county}} \text{pp\_exp\_real}_t \times \text{school\_age\_pop}_t}{\sum_{t \in \text{county}} \text{school\_age\_pop}_t}
$$

where:
- $t$ indexes tracts within the county
- $\text{pp\_exp\_real}_t$ = inflation-adjusted per-pupil expenditure for tract $t$
- $\text{school\_age\_pop}_t$ = school-age population (ages 5-17) in tract $t$

With inflation-adjusted county-level spending in hand, we can now merge the reform treatment data in Stage 4.3.

The Stata command:
```stata
collapse (mean) pp_exp_real [w = school_age_pop], by(county year4)
```

This produces `county_exp` (weighted average PPE) and `school_age_pop` (total) for each county-year.

### Stage 4.2 Output: county_panel_interp.dta

**Table 13: Stage 4.2 Output Variables**

| Variable | Description |
|----------|-------------|
| `county` | State + County FIPS (5 chars) |
| `year4` | Fiscal year |
| `county_exp` | Enrollment-weighted average PPE (2000$) |
| `school_age_pop` | Total school-age population |
| `county_name` | County name (cleaned) |
| `state_fips` | State FIPS code |
| `year_unified` | Calendar year (year4 - 1) |

### Stage 4.2 Pipeline Diagram {#stage4-2-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    INTERP_T["<b>tract_panel_interp.dta</b><br/>Tract panel from 4.1"]
    NHGIS_TRACT["<b>NHGIS Tract</b><br/>enroll_age_tract.csv<br/>School-age population"]
    NHGIS_COUNTY["<b>NHGIS County</b><br/>enroll_age_county.csv<br/>School-age population"]

    INFLATE_LABEL["<b>INFLATION ADJUSTMENT</b>"]
    INFLATE_OP["pp_exp_real = pp_exp × inflator_2000<br/>Using state fiscal year CPI"]

    INTERP_T_REAL["<b>tract_panel_interp_real.dta</b><br/>Real spending (2000$)"]

    COLLAPSE_LABEL["<b>COLLAPSE</b>"]
    COLLAPSE_OP["school_age_pop<br/>by tract70"]

    MERGE_LABEL["<b>MERGE</b>"]

    DROP_MERGE["drop if _merge = 1"]

    SPLIT["Split by no_tract"]

    NO_TRACT_0["<b>no_tract = 0</b><br/>(tracted areas)"]
    NO_TRACT_1["<b>no_tract = 1</b><br/>(untracted areas)"]

    GRF_TRACT_POP["<b>grf_tract_school_age_pop_v1</b><br/>Tracted with tract-level pop"]

    MERGE_COUNTY["<b>MERGE</b><br/>county-level pop"]

    GRF_NO_TRACT["<b>grf_no_tract</b><br/>Untracted with county-level pop"]

    APPEND_LABEL["<b>APPEND</b>"]

    RESHAPE_LABEL["<b>RESHAPE WIDE</b>"]
    RESHAPE_OP["by sdtc (district type)<br/>Creates separate columns"]

    PPE_LABEL["<b>ASSIGN TRACT PPE</b>"]
    PPE_OP["Weighted by primary/secondary shares<br/>or fallback to single district"]

    FIX_UNTR["<b>FIX UNTRACTED POP</b>"]
    FIX_OP["Type 3/4: Compute residual<br/>county_total - tract_sum"]

    COLLAPSE_COUNTY["<b>COLLAPSE TO COUNTY</b>"]
    COLLAPSE_COUNTY_OP["collapse (mean) pp_exp_real<br/>[w = school_age_pop]<br/>by county year4"]

    OUTPUT["<b>county_panel_interp.dta</b><br/>County-year panel<br/>county_exp, school_age_pop"]

    %% Flow
    INTERP_T --> INFLATE_LABEL
    INFLATE_LABEL --> INFLATE_OP
    INFLATE_OP --> INTERP_T_REAL

    NHGIS_TRACT --> COLLAPSE_LABEL
    COLLAPSE_LABEL --> COLLAPSE_OP

    INTERP_T_REAL --> MERGE_LABEL
    COLLAPSE_OP --> MERGE_LABEL
    MERGE_LABEL --> DROP_MERGE
    DROP_MERGE --> SPLIT

    SPLIT --> NO_TRACT_0
    SPLIT --> NO_TRACT_1

    NO_TRACT_0 --> GRF_TRACT_POP

    NO_TRACT_1 --> MERGE_COUNTY
    NHGIS_COUNTY --> MERGE_COUNTY
    MERGE_COUNTY --> GRF_NO_TRACT

    GRF_TRACT_POP --> APPEND_LABEL
    GRF_NO_TRACT --> APPEND_LABEL

    APPEND_LABEL --> RESHAPE_LABEL
    RESHAPE_LABEL --> RESHAPE_OP
    RESHAPE_OP --> PPE_LABEL
    PPE_LABEL --> PPE_OP
    PPE_OP --> FIX_UNTR
    FIX_UNTR --> FIX_OP
    FIX_OP --> COLLAPSE_COUNTY
    COLLAPSE_COUNTY --> COLLAPSE_COUNTY_OP
    COLLAPSE_COUNTY_OP --> OUTPUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000
    classDef splitBox fill:#e1f5fe,stroke:#0288d1,stroke-width:1px,color:#000000

    class INTERP_T,NHGIS_TRACT,NHGIS_COUNTY sourceBox
    class INFLATE_LABEL,COLLAPSE_LABEL,MERGE_LABEL,APPEND_LABEL,RESHAPE_LABEL,MERGE_COUNTY,PPE_LABEL,FIX_UNTR,COLLAPSE_COUNTY stepLabel
    class INFLATE_OP,COLLAPSE_OP,DROP_MERGE,RESHAPE_OP,PPE_OP,FIX_OP,COLLAPSE_COUNTY_OP filterBox
    class INTERP_T_REAL,GRF_TRACT_POP,GRF_NO_TRACT outputBox
    class SPLIT,NO_TRACT_0,NO_TRACT_1 splitBox
    class OUTPUT finalBox
```

---

## Stage 4.3: Reform Treatment Data {#stage4-3}

Stage 4.2 produced a county-year panel with real spending. Stage 4.3 merges school finance reform (SFR) treatment data from Jackson, Johnson, and Persico (2016), hereafter JJP. The reform data identifies when each state's school finance system was overturned by a court and what type of reform followed. This information is essential for the event-study design, which compares spending trajectories before and after reform.

**Source:** `tabula-tabled2.xlsx` (Table D2 from the JJP 2014 NBER Working Paper 20118, extracted via Tabula)

### Understanding School Finance Reforms

School finance reforms occur when courts rule that a state's school funding system violates the state constitution, typically by providing unequal educational opportunities across rich and poor districts. Following such rulings, states implement various reform mechanisms to equalize or increase school funding.

JJP compiled a comprehensive dataset of these court cases and their outcomes. Table D2 (from the 2014 working paper) records the court case name and year, the year the ruling took effect (which may differ from the ruling year), and the reform types implemented after the ruling.

### Step 1: Processing the Reform Table

The JJP table contains multiple court cases for some states (if the original ruling was appealed, modified, or followed by additional litigation). We take the **first court overturn** as the treatment date because:

1. The initial ruling establishes the legal precedent that forces legislative action.
2. Later rulings typically build on or modify the initial reform, not replace it.
3. Using the first ruling provides a consistent definition across states.

The processing involves several steps. First, we import the Excel file. The JJP table is a PDF that we converted to Excel using Tabula. Second, we parse the reform year, extracting the first year the court ruled the system unconstitutional. Third, we keep only the first overturn per state. Fourth, we create reform type flags.

JJP categorizes reforms into five types: minimum foundation plans (MFP), equalization plans (EP), local effort (LE), spending limits (SL), and equity cases (EQ). Reforms are not mutually exclusive. States without court overturns during our study period get `reform_year` = 0 and `never_treated` = 1. These states serve as the control group in our event-study design.

**Table 14: Reform Type Variables**

| Variable | Reform Type |
|----------|-------------|
| `reform_mfp` | Minimum Foundation Plan |
| `reform_ep` | Equalization Plan |
| `reform_le` | Local Effort |
| `reform_sl` | Spending Limits |
| `reform_eq` | Equity |

### Step 2: Merge to County Panel

The reform data has one row per state. We merge it to the county panel using state FIPS codes. This is a many-to-one merge because many counties belong to each state. After the merge, every county-year observation inherits its state's reform year and reform type flags.

### Stage 4.3 Pipeline Diagram {#stage4-3-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    INTERP_C["<b>county_panel_interp.dta</b><br/>County-year panel<br/>from Stage 4.2"]
    JJP["<b>tabula-tabled2.xlsx</b><br/>JJP Reform Table<br/>State × Reform Type"]

    IMPORT_LABEL["<b>IMPORT EXCEL</b>"]
    IMPORT_OP["Import JJP Table D2<br/>Reform dates and types"]

    CLEAN_LABEL["<b>CLEAN</b>"]
    CLEAN_OP["Rename variables<br/>Keep first overturn per state<br/>Set reform_year=0 if never treated"]

    FLAGS_LABEL["<b>CREATE FLAGS</b>"]
    FLAGS_OP["reform_mfp, reform_ep<br/>reform_le, reform_sl, reform_eq"]

    REFORM_DATA["<b>Reform Data</b><br/>One row per state<br/>reform_year + type flags"]

    MERGE_LABEL["<b>MERGE m:1</b>"]
    MERGE_OP["on state_fips"]

    OUTPUT["<b>county_panel_w_reform.dta</b><br/>County-year panel<br/>with reform treatment"]

    %% Flow
    JJP --> IMPORT_LABEL
    IMPORT_LABEL --> IMPORT_OP
    IMPORT_OP --> CLEAN_LABEL
    CLEAN_LABEL --> CLEAN_OP
    CLEAN_OP --> FLAGS_LABEL
    FLAGS_LABEL --> FLAGS_OP
    FLAGS_OP --> REFORM_DATA

    INTERP_C --> MERGE_LABEL
    REFORM_DATA --> MERGE_LABEL
    MERGE_LABEL --> MERGE_OP
    MERGE_OP --> OUTPUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000

    class INTERP_C,JJP sourceBox
    class IMPORT_LABEL,CLEAN_LABEL,FLAGS_LABEL,MERGE_LABEL stepLabel
    class IMPORT_OP,CLEAN_OP,FLAGS_OP,MERGE_OP filterBox
    class REFORM_DATA outputBox
    class OUTPUT finalBox
```

---

## Stage 4.4: Median Income and Output {#stage4-4}

Stage 4.3 added reform treatment data to the county panel. Stage 4.4 completes the county panel by merging median family income from the 1970 Census. Income serves as an important control variable and helps characterize the economic context of each county at baseline.

The income merge is straightforward. We import the county-level income file, clean the county identifiers to ensure they match our panel, and merge on county name and state FIPS code. The merge is many-to-one since income is measured once (in 1970) but the panel has multiple years per county.

With the county panel complete, Stage 5 applies sample restrictions, constructs event-time variables, and prepares the data for regression analysis.

## Attrition

At the end of Stage 2, we had 2,308 unique counties across 46 states. Stage 4 leaves us with 2,290 counties, a loss of 18. These counties were dropped because they could not be merged with school-age population data or median family income data.

**Table 15: Stage 4.4 Output Variables (county_panel.dta)**

| Variable | Description |
|----------|-------------|
| `county` | State + County FIPS (5 chars) |
| `year4` | Fiscal year |
| `county_exp` | Enrollment-weighted average PPE (2000$) |
| `school_age_pop` | Total school-age population |
| `reform_year` | Year of first court overturn (0 if never) |
| `reform_mfp` | Minimum Foundation Program reform flag |
| `reform_ep` | Equalization percentage reform flag |
| `reform_le` | Leveling-up reform flag |
| `reform_sl` | Spending limits reform flag |
| `reform_eq` | Equity-focused reform flag |
| `median_family_income` | County median family income (1970 Census) |
| `state_fips` | State FIPS code |
| `county_name` | County name |

### Stage 4.4 Pipeline Diagram {#stage4-4-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    INTERP_TREAT["<b>county_panel_w_reform.dta</b><br/>County-year panel<br/>with reform treatment"]
    COUNTY2["<b>county2.csv</b><br/>Median family income<br/>by county"]
    STATE_FIPS["<b>state_fips_master.csv</b><br/>State FIPS crosswalk"]

    IMPORT_LABEL["<b>IMPORT CSV</b>"]
    IMPORT_OP["Import median_family_income<br/>Clean county identifiers"]

    MERGE_STATE["<b>MERGE STATE FIPS</b>"]
    MERGE_STATE_OP["Add numeric state codes"]

    INCOME_DATA["<b>Income Data</b><br/>One row per county<br/>median_family_income"]

    MERGE_LABEL["<b>MERGE m:1</b>"]
    MERGE_OP["on county"]

    OUTPUT["<b>county_panel.dta</b><br/>★ FINAL OUTPUT ★<br/>Analysis-ready panel"]

    %% Flow
    COUNTY2 --> IMPORT_LABEL
    IMPORT_LABEL --> IMPORT_OP
    IMPORT_OP --> MERGE_STATE
    STATE_FIPS --> MERGE_STATE
    MERGE_STATE --> MERGE_STATE_OP
    MERGE_STATE_OP --> INCOME_DATA

    INTERP_TREAT --> MERGE_LABEL
    INCOME_DATA --> MERGE_LABEL
    MERGE_LABEL --> MERGE_OP
    MERGE_OP --> OUTPUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000

    class INTERP_TREAT,COUNTY2,STATE_FIPS sourceBox
    class IMPORT_LABEL,MERGE_STATE,MERGE_LABEL stepLabel
    class IMPORT_OP,MERGE_STATE_OP,MERGE_OP filterBox
    class INCOME_DATA outputBox
    class OUTPUT finalBox
```

---

# Stage 5: Sample Restrictions and Final Dataset {#stage5}

Stage 4 produced a county-year panel with spending, reform treatment, and income data. Stage 5 transforms this into the final analysis dataset. This stage performs three critical functions: creating the spending variables used in regressions, constructing event-time indicators for the event-study design, and enforcing balanced panel requirements.

Why do these steps matter? The event-study design requires consistent observation of units before and after treatment. If a county enters or exits our data during the event window, we cannot distinguish treatment effects from compositional changes. The restrictions in Stage 5 ensure that our estimates reflect actual spending responses to reform, not data artifacts.

## Stage 5.1: Primary Balance {#stage5-1}

This substage produces the primary analysis dataset with strict balance requirements. The inputs are `county_panel.dta` from Stage 4.4 and `county_qual_flags.dta` from Stage 3.

### Step 1: Creating Analysis Variables

The first step creates the spending variables we will use in regressions. We construct four versions, each with different properties.

The base variable `exp` is per-pupil expenditure winsorized at the 1st and 99th percentiles by year. Winsorizing replaces extreme values with the percentile cutoffs, bounding the influence of outliers without dropping observations. Raw per-pupil expenditure contains extreme values, some from data errors, others from unusual circumstances like a tiny district with very high spending.

From this we compute `lexp`, the log of winsorized spending. The log transformation enables percentage interpretation: a coefficient of 0.05 means approximately a 5% increase in spending.

We then compute `lexp_ma`, the log of a 13-year rolling mean. School spending fluctuates year-to-year due to budget cycles, one-time expenditures, and accounting timing. The rolling mean smooths this noise by averaging spending from year $t-12$ through year $t$. This choice also mirrors JJP's use of average spending over ages 5–17.

Finally, `lexp_ma_strict` is the log of the rolling mean computed only when all 13 years have data. The regular rolling mean uses whatever years are available; the strict version equals missing if any of the 13 years lacks data. This ensures counties with data gaps are excluded rather than measured with error.

**Table 16: Spending Variables**

| Variable | Definition | Use Case |
|----------|------------|----------|
| `exp` | Winsorized PPE (1st/99th percentile by year) | Removes extreme outliers |
| `lexp` | Log of winsorized PPE | Regression outcome (single-year) |
| `lexp_ma` | Log of 13-year rolling mean | Smoothed outcome |
| `lexp_ma_strict` | Log of rolling mean, **only if full 13-year window exists** | **Primary outcome** |


$$
\text{rollingmean}_t = \frac{1}{13} \sum_{j=0}^{12} \text{exp}_{t-j}
$$

### Step 2: Event-Time Indicators

Event-study regressions use "event time" (years measured relative to the treatment date) rather than calendar time. We calculate relative year as calendar year minus reform year. For example, if Texas reformed in 1989, then 1987 is relative year -2 (two years before reform), 1989 is relative year 0 (the reform year), and 1995 is relative year 6 (six years after reform). For never-treated states, we set relative year to missing since they have no reform year.

We then create indicator variables for each event-time period. The leads capture pre-reform years: `lead_5` equals 1 if relative year is -5 or earlier (binned), `lead_4` through `lead_1` capture years -4 through -1 individually. The lags capture post-reform years: `lag_1` through `lag_16` capture years 1 through 16 individually, and `lag_17` equals 1 if relative year is 17 or later (binned).

Year 0 (the reform year) is omitted as the reference category. All coefficients therefore measure changes relative to spending in the reform year. A coefficient of 0.10 on `lag_5` means spending was 10% higher five years after reform compared to the reform year itself.

**Calculating relative year:**

$$
\text{relative\_year} = \text{calendar\_year} - \text{reform\_year}
$$

**Table 17: Lead and Lag Indicators**

| Variable | Definition | Interpretation |
|----------|------------|----------------|
| `lead_5` | relative_year ≤ -5 | 5+ years before reform (binned) |
| `lead_4` | relative_year = -4 | 4 years before reform |
| `lead_3` | relative_year = -3 | 3 years before reform |
| `lead_2` | relative_year = -2 | 2 years before reform |
| `lead_1` | relative_year = -1 | 1 year before reform |
| Year 0 | relative_year = 0 | **Omitted** (reference period) |
| `lag_1` | relative_year = 1 | 1 year after reform |
| `lag_2` | relative_year = 2 | 2 years after reform |
| ... | ... | ... |
| `lag_16` | relative_year = 16 | 16 years after reform |
| `lag_17` | relative_year ≥ 17 | 17+ years after reform (binned) |


### Step 3: Baseline Quartiles

We classify counties by their pre-reform spending levels to test whether reforms affected low-spending and high-spending areas differently. Quartiles are computed within each state using 1971 (fiscal year 1972) data. For each state, we rank counties by their 1971 per-pupil expenditure and divide into four groups: Q1 (lowest 25%), Q2 (25th–50th percentile), Q3 (50th–75th percentile), and Q4 (highest 25%).

Why 1971? We need a baseline year that predates most reforms (most court cases occurred in the late 1970s onward), has good data coverage, and is close enough to reforms to reflect the pre-reform status quo. The year 1971 satisfies all three criteria.

We also compute income quartiles (`inc_q`) using 1970 Census median family income. This allows us to examine whether reforms helped low-income or high-income areas.

### Step 4: Balanced Panel Restriction

The event-study design requires observing each county continuously across the event window. A county that appears and disappears cannot reliably identify treatment effects; changes in spending might reflect the county entering or exiting the data rather than responding to reform.

A county is "balanced" if `lexp_ma_strict` is non-missing for all 23 years of the event window, from relative year -5 through relative year +17. Why 23 years? The window spans -5, -4, -3, -2, -1, 0, 1, 2, ..., 17, which totals 23 years.

This restriction substantially reduces sample size but ensures clean identification. Counties that fail the balance criterion often have data gaps from missing district-level finance data, from districts that formed or dissolved during the study period, or from incomplete Census tract coverage.

One important caveat: this approach excludes many states with early reforms, since those reforms occur less than 13 years after the first year of available data (fiscal year 1967). The strict rolling mean requires 13 prior years of data, which mechanically excludes early-reforming states from the balanced sample.

$$
\text{balanced} = 1 \iff \text{lexp\_ma\_strict}_{it} \neq \text{missing} \quad \forall t \in [-5, +17]
$$

**Table 18: Balance Requirement Example**

| County | Years with `lexp_ma_strict` | Balanced? |
|--------|---------------------------|-----------|
| Adams (CA) | -5 to +17 (all 23) | Yes |
| Baker (TX) | -5 to +12 (18 years) | No, missing +13 to +17 |
| Clark (NY) | -2 to +17 (missing early years) | No, missing -5 to -3 |

### Step 5: State Filter

Some states have very few counties in our final sample, perhaps only 2 or 3. Computing within-state quartiles with so few observations produces unstable estimates. We flag states with sufficient data:

- `valid_st = 1` if the state has ≥10 counties in the sample
- `valid_st_gd = 1` if the state has ≥10 "good" counties (with complete baseline data)

States failing this criterion remain in the dataset but are excluded from analyses that require within-state quartile comparisons.

### Attrition

The strict balance requirement on `lexp_ma_strict` leaves us with 1,587 counties across 35 states.

**Table 19: Stage 5.1 Output Variables**

| Variable | Description |
|----------|-------------|
| `county_id` | 5-digit FIPS (state + county) |
| `state_fips` | State FIPS code |
| `year` | School year (year4 - 1) |
| `relative_year` | Years since reform |
| `good` | Good county flag (has 1972 baseline data) |
| `exp` | Per-pupil expenditure (winsorized, 2000$) |
| `lexp` | Log per-pupil expenditure |
| `lexp_ma` | Log 13-year rolling mean PPE |
| `lexp_ma_strict` | Log 13-year strict rolling mean PPE |
| `pre_q` | Baseline spending quartile (1971) |
| `inc_q` | Income quartile (1970 Census) |
| `med_fam_inc` | Median family income |
| `school_age_pop` | School-age population (weight) |
| `reform_year` | Year of reform (0 if never) |
| `reform_types` | Reform type grouping |
| `reform_eq`, `reform_mfp`, etc. | Individual reform type flags |
| `lead_1` to `lead_5` | Pre-reform indicators |
| `lag_1` to `lag_17` | Post-reform indicators |
| `valid_st`, `valid_st_gd` | State validity flags |

### Stage 5.1 Pipeline Diagram {#stage5-1-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    COUNTY_EXP["<b>county_panel.dta</b><br/>From Stage 4.4"]
    COUNTY_CLEAN["<b>county_qual_flags.dta</b><br/>Quality flags from Stage 3"]

    MERGE_LABEL["<b>MERGE</b>"]
    MERGE_OP["Merge quality flags<br/>on county"]

    VARS_LABEL["<b>CREATE VARIABLES</b>"]
    VARS_OP["Winsorize exp<br/>lexp = log(exp)<br/>lexp_ma_strict = strict rolling mean<br/>lead/lag indicators<br/>baseline quartiles"]

    INTERP["<b>analysis_panel_unrestricted.dta</b><br/>Full panel before restrictions"]

    BAL_LABEL["<b>BALANCE RESTRICTION</b>"]
    BAL_OP["Keep if:<br/>• relative_year -5 to +17 complete<br/>• lexp_ma_strict non-missing for all 23 yrs"]

    STATE_LABEL["<b>STATE FILTER</b>"]
    STATE_OP["Flag states with ≥10 counties<br/>valid_st, valid_st_gd"]

    OUTPUT["<b>analysis_panel_bal.dta</b><br/>★ ANALYSIS DATASET ★<br/>Balanced on lexp_ma_strict"]

    %% Flow
    COUNTY_EXP --> MERGE_LABEL
    COUNTY_CLEAN --> MERGE_LABEL
    MERGE_LABEL --> MERGE_OP
    MERGE_OP --> VARS_LABEL
    VARS_LABEL --> VARS_OP
    VARS_OP --> INTERP

    INTERP --> BAL_LABEL
    BAL_LABEL --> BAL_OP
    BAL_OP --> STATE_LABEL
    STATE_LABEL --> STATE_OP
    STATE_OP --> OUTPUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000

    class COUNTY_EXP,COUNTY_CLEAN sourceBox
    class MERGE_LABEL,VARS_LABEL,BAL_LABEL,STATE_LABEL stepLabel
    class MERGE_OP,VARS_OP,BAL_OP,STATE_OP filterBox
    class INTERP outputBox
    class OUTPUT finalBox
```

---

## Stage 5.2: Alternative Balance {#stage5-2}

Stage 5.1 imposed strict balance requirements that exclude counties with incomplete rolling-mean histories. Stage 5.2 creates an alternative dataset with relaxed requirements.

The difference is simple: instead of requiring `lexp_ma_strict` to be non-missing for all 23 years, we require only `lexp` (the single-year log spending) to be non-missing. This includes counties that have event-window data but incomplete pre-window histories, such as counties in states with early reforms that cannot compute the full 13-year rolling mean.

**Table 20: Balance Criteria Comparison**

| Dataset | Balance Criterion | Use Case |
|---------|-------------------|----------|
| `analysis_panel_bal.dta` | `lexp_ma_strict` non-missing for 23 years | Strict analysis |
| `analysis_panel_bal_alt.dta` | `lexp` non-missing for 23 years | Larger sample |

### Attrition

The relaxed balance requirement on `lexp` leaves us with 1,986 counties across 43 states.

In our code the difference is simply which variable we check for missingness:

```stata
* PRIMARY:   n_nonmiss = sum(!missing(lexp_ma_strict))
* ALTERNATIVE: n_nonmiss = sum(!missing(lexp))
```

---

# Stage 6: Event-Study Analysis {#stage6}

Stage 5 produced balanced analysis datasets with spending variables, event-time indicators, and baseline quartiles. Stage 6 runs the event-study regressions that replicate Figure 1 from JJP (2016). These regressions estimate how school spending changed in response to court-ordered finance reforms, and whether those changes differed between initially low-spending and high-spending counties.

**Source:** `analysis_panel_bal.dta` or `analysis_panel_bal_alt.dta` (from Stage 5)

## Regression Specification

The event-study design estimates spending trajectories before and after reform by regressing log per-pupil expenditure on a set of event-time indicators. The model takes the form:

```
lexp_ma_strict = α + Σ βₖ·lag_k + Σ γₖ·lead_k + δ·year + county_FE + ε

```

**Table 21: Regression Specification Components**

| Component | Implementation |
|-----------|----------------|
| Outcome | `lexp`, `lexp_ma`, or `lexp_ma_strict` |
| Fixed effects | County (`absorb(county_id)`) + Year (`i.year`) |
| Clustering | County-level (`vce(cluster county_id)`) |
| Weights | School-age population (`[w=school_age_pop]`) |
| Sample | `good==1`, `valid_st_gd==1`, `reform_year<2000` |

The outcome variable is the log of the 13-year rolling mean of per-pupil expenditure, though we also estimate specifications using single-year log spending (`lexp`) and the non-strict rolling mean (`lexp_ma`). The key regressors are the lead and lag indicators constructed in Stage 5, which capture spending in each year relative to the reform date. Year 0 (the reform year) is omitted as the reference category, so all coefficients measure changes relative to spending in the reform year.

The model includes county fixed effects (absorbed via `areg`) and year fixed effects. County fixed effects control for time-invariant differences across counties, while year fixed effects control for national trends in school spending that affect all counties equally. Standard errors are clustered at the county level to account for correlation within counties over time. Observations are weighted by school-age population so that larger counties contribute more to the estimates.

The sample restrictions ensure we include only counties with complete baseline data (`good==1`), in states with at least 10 valid counties (`valid_st_gd==1`), and with reform dates early enough to observe post-reform outcomes (`reform_year<2000`).

## Analysis Structure

**By Quartile.** We run separate regressions for each baseline spending quartile (Q1=poorest to Q4=richest). This reveals whether reforms differentially affected poor vs. rich districts.

**Pooled Q1-Q3.** We pool the bottom three quartiles to test overall effects on non-wealthy districts.

## Output: Event-Study Plots

The code extracts coefficients via `lincom`, computes 90% confidence intervals, and generates plots showing:

- **X-axis:** Years since reform (-5 to +17)
- **Y-axis:** Coefficient (change in log PPE relative to year 0)
- **Shaded area:** 90% confidence interval

**Table 22: Interpreting Event-Study Plots**

| Pattern | Interpretation |
|---------|----------------|
| Flat pre-trends (leads ≈ 0) | Parallel trends assumption holds |
| Positive lags | Spending increased after reform |
| Larger Q1 effects than Q4 | Reforms helped poorer districts more |

### Stage 6 Pipeline Diagram {#stage6-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '13px', 'fontFamily': 'arial'}}}%%
flowchart TB
    INPUT["<b>analysis_panel_bal.dta</b><br/>or analysis_panel_bal_alt.dta"]

    OPTION["<b>OPTIONS</b>"]
    OPTION_BOX["use_alt: 0 or 1<br/>outcomes: lexp, lexp_ma, lexp_ma_strict"]

    PART_A["<b>PART A: BY QUARTILE</b>"]
    Q1["<b>Q1</b><br/>Poorest districts"]
    Q2["<b>Q2</b>"]
    Q3["<b>Q3</b>"]
    Q4["<b>Q4</b><br/>Richest districts"]

    AREG["<b>areg</b><br/>outcome ~ lead/lag + year<br/>absorb(county_id)<br/>cluster(county_id)<br/>weight=school_age_pop"]

    COEF["<b>EXTRACT COEFFICIENTS</b>"]
    COEF_OP["lincom for each lead/lag<br/>Compute 90% CI"]

    PLOT_Q["<b>PLOTS BY QUARTILE</b>"]
    PLOT_Q_OUT["fig1_outcome_q1.png<br/>fig1_outcome_q2.png<br/>fig1_outcome_q3.png<br/>fig1_outcome_q4.png"]

    PART_B["<b>PART B: Q1-Q3 POOLED</b>"]
    POOL["<b>Pooled regression</b><br/>pre_q < 4"]

    PLOT_POOL["<b>POOLED PLOT</b>"]
    PLOT_POOL_OUT["fig1_outcome_q123.png"]

    %% Flow
    INPUT --> OPTION
    OPTION --> OPTION_BOX
    OPTION_BOX --> PART_A

    PART_A --> Q1
    PART_A --> Q2
    PART_A --> Q3
    PART_A --> Q4

    Q1 --> AREG
    Q2 --> AREG
    Q3 --> AREG
    Q4 --> AREG

    AREG --> COEF
    COEF --> COEF_OP
    COEF_OP --> PLOT_Q
    PLOT_Q --> PLOT_Q_OUT

    OPTION_BOX --> PART_B
    PART_B --> POOL
    POOL --> AREG
    COEF_OP --> PLOT_POOL
    PLOT_POOL --> PLOT_POOL_OUT

    %% Styling
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stepLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef filterBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000
    classDef quartileBox fill:#e1f5fe,stroke:#0288d1,stroke-width:1px,color:#000000

    class INPUT sourceBox
    class OPTION,PART_A,PART_B,COEF,PLOT_Q,PLOT_POOL stepLabel
    class OPTION_BOX,AREG,COEF_OP filterBox
    class Q1,Q2,Q3,Q4,POOL quartileBox
    class PLOT_Q_OUT,PLOT_POOL_OUT finalBox
```

---

# Sample Attrition {#sample-attrition}

**Table 23: Sample Attrition by Stage**

| Stage | Districts | Counties | States |
|-------|-----------|----------|--------|
| F-33 panel (LEAID universe) | 24,700 | 3,441 | 51 |
| Stage 1: After 1:1 filter | 15,047 | 2,813 | 47 |
| Stage 1: After GRF merge | 9,428 | 2,548 | 46 |
| Stage 2: After tract assignment | 5,760 | 2,308 | 46 |
| Stage 4: After income merge | — | 2,290 | 46 |
| Stage 5.1: Strict balance (`lexp_ma_strict`) | — | 1,587 | 35 |
| Stage 5.2: Alt balance (`lexp`) | — | 1,986 | 43 |

The pipeline retains 38% of the original districts in the F-33 ID file. However, of the 12,368 unique districts in the GRF, 9,428 (76%) successfully match our district panel. Attrition occurs for several reasons. Districts with ambiguous identifier mappings (many-to-one or one-to-many LEAID-GOVID relationships) are dropped to ensure consistent tracking over time. Districts not appearing in the 1969 Geographic Reference File cannot be linked to Census geography. JJP note the absence of Alaska, Hawaii, Maryland, North Carolina, Virginia, and Washington DC from the historical data prior to 1992, which contributes to attrition as well. Finally, the balanced panel requirement in Stage 5 further reduces the sample by excluding counties that lack complete data across the 23-year event window.

---

# Summary

The pipeline transforms raw school finance data into event-study estimates through six stages:

**Table 24: Pipeline Summary**

| Stage | Input | Output | Key Operation |
|-------|-------|--------|---------------|
| 1 | F-33, INDFIN, GRF | `dist_panel` | Build LEAID↔GOVID crosswalk, unify panels |
| 2 | District panel, GRF | `tract_panel` | Assign districts to tracts by population |
| 3 | Tract panel | `county_qual_flags` | Tag counties by baseline data quality |
| 4 | Tract panel, NHGIS, JJP | `county_panel` | Interpolate, adjust inflation, merge reforms |
| 5 | County panel | `analysis_panel_bal` | Apply balance restrictions, create analysis vars |
| 6 | Analysis dataset | Figure 1 plots | Run event-study regressions by quartile |


---

# Complete Pipeline Diagram {#full-diagram}

```{mermaid}
%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'primaryTextColor': '#000000', 'primaryBorderColor': '#333333', 'lineColor': '#666666', 'secondaryColor': '#f5f5f5', 'tertiaryColor': '#fafafa', 'fontSize': '11px', 'fontFamily': 'arial'}}}%%
flowchart TB
    %% ===== RAW DATA SOURCES =====
    F33["<b>F-33 Panel</b><br/>1992-2019<br/>LEAID + GOVID"]
    INDFIN["<b>INDFIN</b><br/>1967-1991<br/>GOVID only"]
    F33ID["<b>F-33 ID File</b><br/>Crosswalk source"]
    GRF["<b>GRF 1969</b><br/>Census geography"]
    NHGIS_T["<b>NHGIS Tract</b><br/>School-age pop"]
    NHGIS_C["<b>NHGIS County</b><br/>School-age pop"]
    JJP["<b>JJP Table D2</b><br/>Reform data"]
    COUNTY_INC["<b>County Income</b><br/>1970 Census"]

    %% ===== STAGE 1: DISTRICT PANEL =====
    S1["<b>STAGE 1</b><br/>District Panel"]
    S1_XWALK["Build 1:1<br/>LEAID↔GOVID<br/>crosswalk"]
    S1_MERGE["Merge INDFIN<br/>+ F-33"]
    S1_GRF["Link to<br/>Census tracts"]
    DIST_PANEL["<b>dist_panel</b><br/>District-year panel"]

    %% ===== STAGE 2: TRACT PANEL =====
    S2["<b>STAGE 2</b><br/>Tract Panel"]
    S2_ASSIGN["Assign LEAID<br/>per tract<br/>(by alloc_pop)"]
    S2_JOIN["Join district<br/>spending"]
    TRACT_PANEL["<b>tract_panel</b><br/>Tract-year panel"]

    %% ===== STAGE 3: COUNTY QUALITY =====
    S3["<b>STAGE 3</b><br/>County Quality"]
    S3_COLLAPSE["Collapse using MIN<br/>(conservative)"]
    COUNTY_CLEAN["<b>county_qual_flags</b><br/>Quality flags"]

    %% ===== STAGE 4: COUNTY PANEL =====
    S4["<b>STAGE 4</b><br/>County Panel"]
    S4_1["<b>4.1</b> Interpolate<br/>+ tract assign"]
    S4_2["<b>4.2</b> Inflation adj<br/>+ collapse"]
    S4_3["<b>4.3</b> Merge<br/>reforms"]
    S4_4["<b>4.4</b> Merge<br/>income"]
    COUNTY_EXP["<b>county_panel</b><br/>County-year panel"]

    %% ===== STAGE 5: SAMPLE RESTRICTIONS =====
    S5["<b>STAGE 5</b><br/>Sample Restrictions"]
    S5_VARS["Create variables:<br/>rolling mean, leads/lags,<br/>quartiles"]
    S5_BAL["Balance restriction<br/>(-5 to +17)"]
    S5_STATE["State filter<br/>(≥10 counties)"]
    JJP_FINAL["<b>analysis_panel_bal</b><br/>Analysis dataset"]
    JJP_ALT["<b>analysis_panel_bal_alt</b><br/>(relaxed balance)"]

    %% ===== STAGE 6: ANALYSIS =====
    S6["<b>STAGE 6</b><br/>Event-Study"]
    S6_REG["areg with<br/>county + year FE"]
    S6_PLOT["Extract coefficients<br/>+ plot"]
    FIGURES["<b>Figure 1 Plots</b><br/>By quartile + pooled"]

    %% ===== CONNECTIONS =====
    %% Stage 1
    F33ID --> S1
    F33 --> S1
    INDFIN --> S1
    GRF --> S1
    S1 --> S1_XWALK
    S1_XWALK --> S1_MERGE
    S1_MERGE --> S1_GRF
    S1_GRF --> DIST_PANEL

    %% Stage 2
    DIST_PANEL --> S2
    S2 --> S2_ASSIGN
    S2_ASSIGN --> S2_JOIN
    S2_JOIN --> TRACT_PANEL

    %% Stage 3
    TRACT_PANEL --> S3
    S3 --> S3_COLLAPSE
    S3_COLLAPSE --> COUNTY_CLEAN

    %% Stage 4
    DIST_PANEL --> S4
    S4 --> S4_1
    S4_1 --> S4_2
    NHGIS_T --> S4_2
    NHGIS_C --> S4_2
    S4_2 --> S4_3
    JJP --> S4_3
    S4_3 --> S4_4
    COUNTY_INC --> S4_4
    S4_4 --> COUNTY_EXP

    %% Stage 5
    COUNTY_EXP --> S5
    COUNTY_CLEAN --> S5
    S5 --> S5_VARS
    S5_VARS --> S5_BAL
    S5_BAL --> S5_STATE
    S5_STATE --> JJP_FINAL
    S5_STATE --> JJP_ALT

    %% Stage 6
    JJP_FINAL --> S6
    JJP_ALT --> S6
    S6 --> S6_REG
    S6_REG --> S6_PLOT
    S6_PLOT --> FIGURES

    %% ===== STYLING =====
    classDef sourceBox fill:#e3f2fd,stroke:#1565c0,stroke-width:2px,color:#000000
    classDef stageLabel fill:#fff3e0,stroke:#ef6c00,stroke-width:2px,color:#000000
    classDef stepBox fill:#fff8e1,stroke:#ffa000,stroke-width:1px,color:#000000
    classDef outputBox fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px,color:#000000
    classDef finalBox fill:#c8e6c9,stroke:#1b5e20,stroke-width:3px,color:#000000

    class F33,INDFIN,F33ID,GRF,NHGIS_T,NHGIS_C,JJP,COUNTY_INC sourceBox
    class S1,S2,S3,S4,S5,S6 stageLabel
    class S1_XWALK,S1_MERGE,S1_GRF,S2_ASSIGN,S2_JOIN,S3_COLLAPSE,S4_1,S4_2,S4_3,S4_4,S5_VARS,S5_BAL,S5_STATE,S6_REG,S6_PLOT stepBox
    class DIST_PANEL,TRACT_PANEL,COUNTY_CLEAN,COUNTY_EXP,JJP_FINAL,JJP_ALT outputBox
    class FIGURES finalBox
```
